'use client';

// This component is currently not used on the main page, 
// as the header is now integrated directly into `src/app/page.tsx`.
// It can be repurposed for other pages or removed if not needed.
export default function Header() {
  return null;
}
