'use client';

import {
  Sidebar,
  SidebarContent,
} from '@/components/ui/sidebar';

export default function AppSidebar() {

  return (
    <Sidebar collapsible="icon">
      <SidebarContent>
        {/* Konten sidebar sengaja dikosongkan sesuai permintaan */}
      </SidebarContent>
    </Sidebar>
  );
}
